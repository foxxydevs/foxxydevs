import React from 'react';

type ArtboardProps = {
  coverImage?: string;
  altText?: string;
  imgStart?: boolean;
};

const ArboardDetail: React.FC<ArtboardProps> = (props) => {
  return <></>;
};

export default ArboardDetail;
